/**
 *  
 * First site you come to after you click to organise a session
 * It will be a 4 step process
 * 1. Select a view from Airtable
 * 2. Write a call script
 * 3. Write a voicemail/SMS message
 * 4. Share the join link
 * 
 * This component should handle the logic for moving between the steps,
 * when to display what step and going back between steps
 * We can have global store co-located here which can assist the component so we don't need to prop drill
 */

export const Organiser = () => {
  return <div>Organiser</div>;
};
