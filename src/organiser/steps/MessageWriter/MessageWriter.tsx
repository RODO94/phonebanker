/**
 * 
 * This component should display a textarea for the user to write a voicemail/SMS message
 * The figma design is here: https://www.figma.com/design/2bSUA8wuI0nY6E3eN0Cesd/Phonebanker?node-id=0-150&m=dev
 * A user should be able to write a voicemail/SMS message and see a preview of it below the textarea
 * It should have simple markdown formatting - bold, italic, lists
 * A user should be able to indicate a variable 'Name' where the name of the person calling should be inserted
 * And another where their name should be inserted
 */

export const MessageWriter = () => {
  return <div>MessageWriter</div>;
};