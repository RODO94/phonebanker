// Placeholder — Segment B1 (Entry screens). No Figma node — honest copy when
// session.status !== 'active' or the link expired. Route to the organiser.
export function SessionEnded() {
  return (
    <div className="screen-placeholder" data-screen="session-ended">
      SessionEnded (Segment B1)
    </div>
  );
}
