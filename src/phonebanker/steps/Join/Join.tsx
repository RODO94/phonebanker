// Placeholder — Segment B1 (Entry screens). Figma node 0-216.
// Member search: 6-char min, 2s debounce, top-5, two distinct zero-result messages.
export function Join() {
  return (
    <div className="screen-placeholder" data-screen="join">
      Join — member search (Segment B1)
    </div>
  );
}
