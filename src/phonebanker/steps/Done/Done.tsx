// Placeholder — Segment B2 (Call loop). Figma node 0-364.
// Celebration screen when the list is exhausted: total calls, how many picked up.
export function Done() {
  return (
    <div className="screen-placeholder" data-screen="done">
      Done (Segment B2)
    </div>
  );
}
