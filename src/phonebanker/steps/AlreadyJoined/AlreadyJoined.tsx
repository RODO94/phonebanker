// Placeholder — Segment B1 (Entry screens). Figma node 0-392.
// Reached when a re-join finds the participant already holds a claim (two devices
// / tab-crash recovery). See users-and-journeys.md "uses two devices at once".
export function AlreadyJoined() {
  return (
    <div className="screen-placeholder" data-screen="already-joined">
      Already Joined (Segment B1)
    </div>
  );
}
