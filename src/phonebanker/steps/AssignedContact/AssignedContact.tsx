// Placeholder — Segment B2 (Call loop). Figma node 0-236.
// The contact card: tel: link, script, copy-message, OutcomeButtons (node 0-290).
export function AssignedContact() {
  return (
    <div className="screen-placeholder" data-screen="assigned-contact">
      AssignedContact (Segment B2)
    </div>
  );
}
