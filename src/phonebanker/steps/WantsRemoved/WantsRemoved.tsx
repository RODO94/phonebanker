// Placeholder — Segment B2 (Call loop). Figma node 0-342 (ContactDoesn'tWantCalled).
// Confirms "wants to be removed" is a FLAG for the organiser, not an auto-removal —
// copy must say so (feature-cheatsheet § Outcome capture).
export function WantsRemoved() {
  return (
    <div className="screen-placeholder" data-screen="wants-removed">
      Wants to be removed (Segment B2)
    </div>
  );
}
